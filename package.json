{
  "name": "tabbit",
  "version": "0.1.0",
  "private": true,
  "description": "A dependency-free Chrome extension for native tab grouping and session cleanup.",
  "type": "commonjs",
  "scripts": {
    "check": "node --check src/organizer.js && node --check src/popup.js",
    "icons": "node scripts/generate-icons.js",
    "test": "node --test tests/*.test.js",
    "validate": "node -e \"JSON.parse(require('node:fs').readFileSync('manifest.json', 'utf8'))\" && npm run check && npm test"
  }
}
